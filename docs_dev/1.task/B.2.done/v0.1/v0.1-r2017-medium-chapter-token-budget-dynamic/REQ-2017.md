---
description: "REQ-2017 单章 Token 预算动态化 — 需求工作副本"
---

# REQ-2017 单章 Token 预算动态化

> 版本：v1.0
> 创建时间：2026-06-26T23:30:00+08:00
> 最后更新：2026-06-26T23:30:00+08:00

---

## 1. 修复方案

### 1.1 服务端：动态阈值计算

在 `DirectorCircuitBreakerService.ts` 中：

```typescript
// 替换硬编码常量
export function computeChapterTotalTokenLimit(targetWordCount?: number | null): number {
  const minLimit = 120_000;
  const wordMultiplier = 20;
  if (typeof targetWordCount === "number" && targetWordCount > 0) {
    return Math.max(targetWordCount * wordMultiplier, minLimit);
  }
  return minLimit;
}
```

### 1.2 服务端：渐进超限处理

`recordChapterUsageBudgetExceededSignal` 增加渐进逻辑：

```typescript
export function recordChapterUsageBudgetExceededSignal(input: {
  previous?: DirectorCircuitBreakerState | null;
  usageRecordId?: string | null;
  totalTokens: number;
  chapterId?: string | null;
  chapterOrder?: number | null;
  nodeKey?: string | null;
  targetWordCount?: number | null;  // ← 新增
}): DirectorCircuitBreakerState | null {
  const limit = computeChapterTotalTokenLimit(input.targetWordCount);
  if (input.totalTokens < limit) {
    return null;
  }
  // 去重
  if (input.usageRecordId && input.previous?.lastUsageRecordId === input.usageRecordId) {
    return input.previous ?? null;
  }
  const previousCount = input.previous?.reason === "usage_anomaly"
    ? input.previous?.usageAnomalyCount ?? 0
    : 0;
  const usageAnomalyCount = previousCount + 1;
  const message = `单章 AI 用量达到 ${input.totalTokens} Tokens（阈值 ${limit}），已暂停以避免继续异常消耗。`;

  // 第1次超限：只记录，不中断
  if (usageAnomalyCount < DIRECTOR_CIRCUIT_BREAKER_THRESHOLDS.usageAnomalyOpenAt) {
    return {
      status: "closed",
      reason: "usage_anomaly",
      message,
      chapterId: input.chapterId ?? null,
      chapterOrder: input.chapterOrder ?? null,
      nodeKey: input.nodeKey ?? "chapter_execution_node",
      patchFailureCount: input.previous?.patchFailureCount ?? 0,
      replanLoopCount: input.previous?.replanLoopCount ?? 0,
      modelFailureCount: input.previous?.modelFailureCount ?? 0,
      usageAnomalyCount,
      lastUsageRecordId: input.usageRecordId ?? null,
      lastEventAt: new Date().toISOString(),
    };
  }
  // 第2次超限：打开断路器
  return openDirectorCircuitBreaker({...});
}
```

> **决策偏差说明**：原需求期望 `recordChapterUsageBudgetExceededSignal` 本身内置渐进计数（第1次记录event不中断，第2次才打开断路器），但审计发现该函数原本就只被 `resolveUsageCircuitBreaker` 调用，且调用方已有 `usageAnomalyOpenAt` 计数机制。原设计中的渐进方案与实际代码结构一致——`usageAnomalyCount` 累积到阈值后才触发 `openDirectorCircuitBreaker`，因此可直接沿用此机制而无需额外 event 记录层。若首版验证发现需要更细粒度的事件留痕，再在 design 迭代中补充。

### 1.3 调用方传参

`resolveUsageCircuitBreaker` in `novelDirectorAutoExecutionCircuitBreakerRuntime.ts` 传入 `targetWordCount`：
- 从 `input.autoExecution` 或查询章节 `targetWordCount` 字段获取

## 2. 涉及文件

| 文件 | 变更 |
|------|------|
| `server/src/services/novel/director/runtime/DirectorCircuitBreakerService.ts` | 增加 `computeChapterTotalTokenLimit`，`recordChapterUsageBudgetExceededSignal` 改为动态阈值 |
| `server/src/services/novel/director/automation/novelDirectorAutoExecutionCircuitBreakerRuntime.ts` | `resolveUsageCircuitBreaker` 传入 `targetWordCount` |
| `server/tests/services/novel/director/DirectorCircuitBreakerService.test.ts` | 新增阈值计算单测 |
