---
description: "v0.1 全量任务包开发 — 分阶段需求追踪清单"
date: 2026-06-27
---

# v0.1 全量任务包开发 — 分阶段需求追踪清单

> 基线：`docs_dev/1.task/requirements.md`（19 个任务包，22 条记录）
> 当前进度：6/19 已完成（Block 1-5 已提交），Block 6 进行中，Block 7-8 待开发
> 总计：19 个任务包（11 P0 + 5 P1 + 3 P2）

---

## 零、阶段总览

```text
Block 1（DB 基础，串行）        Block 2（共享类型，并行）      Block 3（服务端小修，并行）
┌─ 2005 章节软删除   ─┐       ┌─ 2006 卷章数调整  ─┐       ┌─ 2003 同步状态重置  ─┐
└─ 2008 上下文窗口    ─┘       └─ 3001 任务批量归档 ─┘       └─ 2007 节奏板上下文  ─┘
  🔒 测试闸门                    🔒 测试闸门                    🔒 测试闸门

Block 4（客户端 UI，并行）     Block 5（断路器，串行）        Block 6（Creative Hub）
┌─ 2009 厂商筛选      ─┐       ┌─ 2017 Token动态化  ─┐       ┌─ 2004 可观测性增强  ─┐
├─ 2010 边栏折叠      ─┤       └─ 2002 流式死循环    ─┘       └──────────────────────┘
└─ 2011 暗色主题      ─┘         🔒 测试闸门                    🔒 测试闸门
  🔒 测试闸门

Block 7（导演运行时，混合）    Block 8（小说扩展，并行）      版本收口
┌─ 2012 暂停参数      ─┐       ┌─ 2001 导入导出      ─┐       🔒 全量 typecheck
├─ 2016 重试入口      ─┤       ├─ 3002 Checklist     ─┤       🔒 全量 test
│                     │       └─ 3005 对话存档      ─┘       🔒 requirements.md 同步
├─ 2015 用户指导修复  ─┤         🔒 测试闸门                  🔒 全部提交
└─ 3003 修复进度弹窗  ─┘
  🔒 测试闸门
```

**闸门规则**：每个 Block 结束后必须运行 `pnpm typecheck && pnpm test`，全部通过方可进入下一 Block。

---

## 一、Block 1：DB 基础（串行，预计 2h）

> 前置条件：无
> 目标：完成 Prisma schema 变更（2 个 migration），为后续 Block 奠定 DB 基础
> 串行说明：两个任务都修改 `prisma/schema.prisma`，必须串行以避免 migration 冲突

### 1.1 REQ-2005 步骤 6 新建章节删除与误操作恢复

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2005-simple-chapter-delete-and-undo` |
| 优先级 | P0 |
| 工时 | 1.5h（12 tasks） |
| 核心改动 | Chapter 表新增 `deletedAt` 软删除字段 + 删除/恢复 API + 前端删除按钮 + undo toast |

**变更清单：**

- [x] T1 Prisma schema：Chapter 模型新增 `deletedAt DateTime?`
- [x] T2 后端：ChapterService 新增 `softDeleteChapter` / `restoreChapter`
- [x] T3 后端：章节列表查询默认过滤 `deletedAt: null`
- [x] T4 后端：`volumePlanChangeDetection` 排除已删除章节
- [x] T5 前端：API 层新增 `deleteNovelChapter` / `restoreNovelChapter`
- [x] T6 前端：mutation hooks 新增删除/恢复
- [x] T7 前端：ChapterExecutionActionPanel 新增删除按钮 + 确认对话框
- [x] T8 前端：undo toast（10 秒撤销）
- [x] T9 前端：删除后自动选中相邻章节
- [x] T10 测试：软删除 + 恢复 API 单元测试

**验收标准：**

- [x] 删除章节后 `deletedAt` 非空，章节从列表消失
- [x] 10 秒内可点击 toast 撤销恢复
- [x] 已删除章节不出现在卷规划同步中

### 1.2 REQ-2008 模型上下文窗口配置与自动压缩

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2008-complex-context-window-awareness` |
| 优先级 | P0 |
| 工时 | 2h（14 tasks） |
| 核心改动 | ModelRouteConfig 新增 contextWindow 字段 + 自动压缩触发逻辑 |

**变更清单：**

- [x] T1 Prisma schema：ModelRouteConfig 新增 `contextWindow` 字段
- [x] T2 shared types：新增 contextWindow 相关类型
- [x] T3 后端：LLM factory 读取 contextWindow 配置
- [x] T4 后端：上下文窗口感知的 token 计数
- [x] T5 后端：自动压缩触发逻辑
- [x] T6 前端：ModelRouteFields 新增 contextWindow 输入控件
- [x] T7 测试：contextWindow 配置读写单元测试

**验收标准：**

- [x] ModelRouteConfig 可配置 contextWindow
- [x] LLM 调用时使用配置的 contextWindow
- [x] 超出窗口时触发自动压缩

### 1.3 复核实际完成度

- [x] 逐项核对变更清单完成情况
- [x] 核对验收标准全部达成
- [x] 更新任务包 `tasks.md` 标记完成
- [x] 更新任务包 `README.md` 状态

### 1.4 Block 1 测试闸门

```bash
pnpm typecheck                         # 类型检查
pnpm test                              # 后端单元测试
```

- [x] 类型检查零错误
- [x] 后端单元测试全部通过
- [x] `pnpm db:migrate` 成功
- [x] git commit：`feat(Block1): 2005 章节软删除 + 2008 上下文窗口配置`

### 1.5 归档本阶段任务包

- [x] 任务包从 `B.todo` 迁移至 `B.2.done`
- [x] 同步更新 `docs_dev/1.task/requirements.md`
- [x] 同步更新 `docs_dev/1.task/README.md`
- [x] 同步更新 `docs_dev/INDEX.md`

---

## 二、Block 2：共享类型（并行，预计 1h）

> 前置：Block 1 完成
> 目标：新增共享类型定义，为后续功能提供类型基础
> 并行说明：两个任务修改不同的 shared 类型文件，无依赖

### 2.1 REQ-2006 步骤 4 每卷章节数手动调整

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2006-simple-volume-chapter-count-config` |
| 优先级 | P0 |
| 工时 | 1h（8 tasks） |
| 核心改动 | VolumePlan 新增 `targetChapterCount` 字段 + 卷卡片输入控件 |

**变更清单：**

- [x] T1 shared：VolumePlan 新增 `targetChapterCount`
- [x] T2 后端：`allocateChapterBudgets` 尊重手动覆盖
- [x] T3 前端：卷卡片新增章节数输入控件
- [x] T4 测试：手动覆盖 + 自动分配边界用例

**验收标准：**

- [x] 用户可在卷卡片中手动设置章节数
- [x] 手动设置后自动分配不再覆盖该卷

### 2.2 REQ-3001 任务中心多选与批量归档

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r3001-medium-task-batch-archive` |
| 优先级 | P1 |
| 工时 | 1.5h（10 tasks） |
| 核心改动 | TaskCenterArchive 模型 + 批量归档 API + 前端多选 UI |

**变更清单：**

- [x] T1 shared：新增 TaskCenterArchive 相关类型
- [x] T2 后端：批量归档 API
- [x] T3 前端：任务中心多选 checkbox
- [x] T4 前端：批量操作工具栏
- [x] T5 测试：批量归档 API + UI 交互

**验收标准：**

- [x] 可多选任务并批量归档
- [x] 归档后任务从活跃列表消失

### 2.3 复核实际完成度

- [x] 逐项核对变更清单完成情况
- [x] 核对验收标准全部达成
- [x] 更新任务包 `tasks.md` 标记完成
- [x] 更新任务包 `README.md` 状态

### 2.4 Block 2 测试闸门

```bash
pnpm typecheck
pnpm test
```

- [x] 类型检查零错误
- [x] 单元测试全部通过
- [x] git commit：`feat(Block2): 2006 卷章数调整 + 3001 任务批量归档`

### 2.5 归档本阶段任务包

- [x] 任务包从 `B.todo` 迁移至 `B.2.done`
- [x] 同步更新 `docs_dev/1.task/requirements.md`
- [x] 同步更新 `docs_dev/INDEX.md`

---

## 三、Block 3：服务端小修（并行，预计 1h）

> 前置：Block 1 完成
> 目标：两个独立的服务端修复
> 并行说明：两个任务无共享文件，可并行

### 3.1 REQ-2003 章节细化同步后状态重置修复

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2003-simple-chapter-sync-status-reset` |
| 优先级 | P0 |
| 工时 | 0.5h（4 tasks） |
| 核心改动 | VolumeChapterSyncService 同步后正确重置章节状态 |

**变更清单：**

- [x] T1 后端：修复 `VolumeChapterSyncService` 状态重置逻辑
- [x] T2 测试：同步后状态验证

**验收标准：**

- [x] 同步后章节 `generationState` 正确重置
- [x] 同步后章节可被自动导演重新生成

### 3.2 REQ-2007 节奏板重新生成时注入历史上下文

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2007-simple-beat-sheet-regen-context-injection` |
| 优先级 | P0 |
| 工时 | 1h（7 tasks） |
| 核心改动 | 节奏板重新生成时注入已写章节的剧情摘要 |

**变更清单：**

- [x] T1 后端：Prompt 模板新增历史上下文注入
- [x] T2 后端：节奏板生成 orchestor 传入上下文
- [x] T3 前端：节奏板卡片显示上下文来源提示
- [x] T4 测试：上下文注入验证

**验收标准：**

- [x] 重新生成节奏板时包含已写章节摘要
- [x] 生成的节奏板与已写内容连贯

### 3.3 复核实际完成度

- [x] 逐项核对变更清单完成情况
- [x] 核对验收标准全部达成
- [x] 更新任务包 `tasks.md` 标记完成
- [x] 更新任务包 `README.md` 状态

### 3.4 Block 3 测试闸门

```bash
pnpm typecheck
pnpm test
```

- [x] 类型检查零错误
- [x] 后端测试全部通过
- [x] git commit：`fix(Block3): 2003 同步状态重置 + 2007 节奏板上下文注入`

### 3.5 归档本阶段任务包

- [x] 任务包从 `B.todo` 迁移至 `B.2.done`
- [x] 同步更新 `docs_dev/1.task/requirements.md`
- [x] 同步更新 `docs_dev/INDEX.md`

---

## 四、Block 4：客户端 UI（并行，预计 2h）

> 前置：Block 1 完成
> 目标：三个独立的纯前端 UI 改进
> 并行说明：三个任务无共享文件，全部是纯客户端改动

### 4.1 REQ-2009 模型路由设置页厂商筛选

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2009-simple-model-provider-filter` |
| 优先级 | P1 |
| 工时 | 1h（5 tasks） |
| 核心改动 | ModelRoutesPage 新增厂商下拉筛选 |

**变更清单：**

- [x] T1 前端：ModelRoutesPage 新增厂商筛选 Select
- [x] T2 前端：筛选联动路由表刷新
- [x] T3 测试：筛选功能验证

**验收标准：**

- [x] 可按厂商（deepseek/openai/anthropic 等）筛选模型路由
- [x] 筛选后仅显示对应厂商路由

### 4.2 REQ-2010 步骤 6 右侧边栏折叠

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2010-simple-chapter-execution-sidebar-collapse` |
| 优先级 | P1 |
| 工时 | 0.5h（3 tasks） |
| 核心改动 | ChapterManagementTab 新增侧边栏折叠按钮 |

**变更清单：**

- [x] T1 前端：ChapterManagementTab 新增折叠/展开按钮
- [x] T2 前端：折叠状态持久化到 localStorage

**验收标准：**

- [x] 点击折叠按钮隐藏侧边栏
- [x] 刷新后折叠状态保持

### 4.3 REQ-2011 暗色主题（夜间模式）

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2011-simple-dark-mode` |
| 优先级 | P1 |
| 工时 | 1h（5 tasks） |
| 核心改动 | 全局 CSS 变量 + dark 类切换 + ThemeToggle 组件 |

**变更清单：**

- [x] T1 前端：`index.css` 新增 dark 主题变量
- [x] T2 前端：`AppLayout` 新增主题切换逻辑
- [x] T3 前端：`ThemeToggle` 组件
- [x] T4 前端：主题偏好持久化到 localStorage

**验收标准：**

- [x] 点击 ThemeToggle 切换亮/暗主题
- [x] 所有页面在暗色主题下可读
- [x] 刷新后主题保持

### 4.4 复核实际完成度

- [x] 逐项核对变更清单完成情况
- [x] 核对验收标准全部达成
- [x] 更新任务包 `tasks.md` 标记完成
- [x] 更新任务包 `README.md` 状态

### 4.5 Block 4 测试闸门

```bash
pnpm typecheck
pnpm test:client
```

- [x] 类型检查零错误
- [x] 前端测试全部通过
- [x] git commit：`feat(Block4): 2009 厂商筛选 + 2010 边栏折叠 + 2011 暗色主题`

### 4.6 归档本阶段任务包

- [x] 任务包从 `B.todo` 迁移至 `B.2.done`
- [x] 同步更新 `docs_dev/1.task/requirements.md`
- [x] 同步更新 `docs_dev/INDEX.md`

---

## 五、Block 5：断路器重构（串行，预计 2h）

> 前置：Block 4 完成（依赖 2008 的 contextWindow）
> 目标：Token 预算动态化 + 流式死循环检测
> 串行说明：两个任务都修改 `DirectorCircuitBreakerService.ts`，必须先做 2017 再做 2002

### 5.1 REQ-2017 单章 Token 预算动态化

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2017-medium-chapter-token-budget-dynamic` |
| 优先级 | P0 |
| 工时 | 1.5h（8 tasks） |
| 核心改动 | 动态阈值 MAX(20×字数, 120000) + 渐进计数 |

**变更清单：**

- [x] T1 新增 `computeChapterTotalTokenLimit(targetWordCount)`
- [x] T2 `recordChapterUsageBudgetExceededSignal` 改为渐进式
- [x] T3 `resolveUsageCircuitBreaker` 传入 `targetWordCount`
- [x] T4 单元测试：阈值计算覆盖所有场景

**验收标准：**

- [x] 4000 字章节阈值 = 120,000
- [x] 8000 字章节阈值 = 160,000
- [x] 第 1 次超限记录不中断，第 2 次打开断路器

### 5.2 REQ-2002 流式生成死循环检测与提前打断

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2002-complex-streaming-loop-detector` |
| 优先级 | P0 |
| 工时 | 1.5h（10 tasks） |
| 核心改动 | SSE 流式生成中检测重复模式 + 提前终止 |

**变更清单：**

- [x] T1 后端：死循环检测器（重复 n-gram / 窗口检测）
- [x] T2 后端：集成到章节生成 SSE 管线
- [x] T3 后端：断路器信号集成
- [x] T4 前端：中断提示 UI
- [x] T5 测试：模拟死循环 + 检测验证

**验收标准：**

- [x] 连续 3 次输出相同段落触发检测
- [x] 检测后中断 SSE 流并提示用户
- [x] 中断后任务可恢复

### 5.3 复核实际完成度

- [x] 逐项核对变更清单完成情况
- [x] 核对验收标准全部达成
- [x] 更新任务包 `tasks.md` 标记完成
- [x] 更新任务包 `README.md` 状态

### 5.4 Block 5 测试闸门

```bash
pnpm typecheck
pnpm test
```

- [x] 类型检查零错误
- [x] 断路器 + 死循环检测测试全部通过
- [x] git commit：`feat(Block5): 2017 Token 预算动态化 + 2002 流式死循环检测`

### 5.5 归档本阶段任务包

- [x] 任务包从 `B.todo` 迁移至 `B.2.done`
- [x] 同步更新 `docs_dev/1.task/requirements.md`
- [x] 同步更新 `docs_dev/INDEX.md`

---

## 六、Block 6：Creative Hub 可观测性（单任务，预计 1.5h）

> 前置：Block 1 完成
> 目标：增强 Creative Hub Agent 执行的可观测性
> 说明：独立任务，无其他依赖

### 6.1 REQ-2004 Creative Hub Agent 执行可观测性增强

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2004-complex-creative-hub-observability` |
| 优先级 | P0 |
| 工时 | 1.5h（11 tasks） |
| 核心改动 | Agent 执行 trace 可视化 + Tokens 用量面板 + 步骤时间线 |

**变更清单：**

- [x] T1 shared：新增 trace/step/usage 投影类型
- [x] T2 后端：CreativeHubLangGraph 采集步骤事件
- [x] T3 后端：step trace API
- [x] T4 前端：AgentRunTracePanel 组件
- [x] T5 前端：TokenUsageCard 组件
- [x] T6 前端：StepTimeline 组件
- [x] T7 测试：trace API + 前端渲染

**验收标准：**

- [x] Creative Hub 执行时前端可看到实时步骤
- [x] Token 用量实时展示
- [x] 步骤时间线可展开查看详情

### 6.2 复核实际完成度

- [x] 逐项核对变更清单完成情况
- [x] 核对验收标准全部达成
- [x] 更新任务包 `tasks.md` 标记完成
- [x] 更新任务包 `README.md` 状态

### 6.3 Block 6 测试闸门

```bash
pnpm typecheck
pnpm test
pnpm test:client
```

- [x] 类型检查零错误
- [x] 全量测试通过
- [x] git commit：`feat(Block6): 2004 Creative Hub 可观测性增强`

### 6.4 归档本阶段任务包

- [x] 任务包从 `B.todo` 迁移至 `B.2.done`
- [x] 同步更新 `docs_dev/1.task/requirements.md`
- [x] 同步更新 `docs_dev/INDEX.md`

---

## 七、Block 7：导演运行时（混合，预计 3h）

> 前置：Block 5 完成（依赖 2017 的断路器改造）
> 目标：导演运行时增强 + 章节修复流程改进
> 并行说明：2012∥2016 可并行（无共享文件），2015→3003 串行（共享 ChapterExecutionActionPanel）

### 7.1 REQ-2012 暂停后调整自动导演高级设置面板

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2012-medium-runtime-param-adjust-during-pause` |
| 优先级 | P0 |
| 工时 | 1.5h（10 tasks） |
| 核心改动 | 暂停状态下可调整 runMode/policyMode + 新增高级设置弹窗 |

**变更清单：**

- [ ] T1 shared：新增 `RuntimePolicyUpdateRequest` 类型
- [ ] T2 后端：`updateRuntimePolicy` 端点
- [ ] T3 后端：`DirectorCommandInterpreter` 处理 policy update
- [ ] T4 前端：`DirectorAdvancedSettingsDialog` 组件
- [ ] T5 前端：`TaskCenterRuntimePolicyCard` UI
- [ ] T6 测试：policy update API + UI

**验收标准：**

- [ ] 暂停后可打开高级设置面板
- [ ] 修改 runMode/policyMode 后立即生效
- [ ] 设置持久化到任务 seedPayload

### 7.2 REQ-2016 自动导演失败状态重试入口缺失

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2016-medium-director-failed-retry-entry` |
| 优先级 | P0 |
| 工时 | 1h（6 tasks） |
| 核心改动 | DashboardView 增加 retry action + 面板处理 retry 事件 |

**变更清单：**

- [ ] T1 后端：`DirectorDashboardViewBuilder` failed 模式增加 retry action
- [ ] T2 前端：`resolveDashboardAction` 处理 retry 类型
- [ ] T3 前端：`resume_from_checkpoint` 触发 resume 重试
- [ ] T4 前端：NovelEdit 传递 retry props

**验收标准：**

- [ ] failed 状态面板显示"重试任务"按钮
- [ ] 点击重试直接触发 retry mutation
- [ ] 重试成功后自动切换到 running 状态

### 7.3 REQ-2015 用户指导式章节修复

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2015-medium-user-directed-chapter-repair` |
| 优先级 | P0 |
| 工时 | 1.5h（11 tasks） |
| 核心改动 | 章节审计面板新增"按指导修复"入口 + userInstruction 字段 |

**变更清单：**

- [ ] T1 后端：repair SSE 端点扩展 `userInstruction` 字段
- [ ] T2 后端：修复 prompt 注入用户指导
- [ ] T3 前端：ChapterExecutionActionPanel 新增"按指导修复"入口
- [ ] T4 前端：用户指导输入弹窗
- [ ] T5 测试：userInstruction 传递 + 修复效果验证

**验收标准：**

- [ ] 用户可输入自由文本修改方向
- [ ] AI 按用户指导执行定向修复
- [ ] 修复结果明显反映用户意图

### 7.4 REQ-3003 章节修复进度感知弹窗

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r3003-medium-repair-progress-dialog` |
| 优先级 | P0 |
| 工时 | 1h（7 tasks） |
| 核心改动 | 修复过程中显示进度弹窗 + 实时内容预览 |

**变更清单：**

- [ ] T1 前端：RepairProgressDialog 组件
- [ ] T2 前端：修复进度百分比 + 实时内容预览
- [ ] T3 前端：ChapterExecutionActionPanel 集成
- [ ] T4 测试：弹窗显示 + 关闭逻辑

**验收标准：**

- [ ] 修复启动后弹出进度弹窗
- [ ] 实时显示修复进度和内容变化
- [ ] 修复完成后弹窗自动关闭

### 7.5 复核实际完成度

- [ ] 逐项核对变更清单完成情况
- [ ] 核对验收标准全部达成
- [ ] 更新任务包 `tasks.md` 标记完成
- [ ] 更新任务包 `README.md` 状态

### 7.6 Block 7 测试闸门

```bash
pnpm typecheck
pnpm test
pnpm test:client
```

- [ ] 类型检查零错误
- [ ] 全量测试通过
- [ ] git commit

### 7.7 归档本阶段任务包

- [ ] 任务包从 `B.todo` 迁移至 `B.2.done`
- [ ] 同步更新 `docs_dev/1.task/requirements.md`
- [ ] 同步更新 `docs_dev/INDEX.md`

---

## 八、Block 8：小说功能扩展（并行，预计 3h）

> 前置：Block 7 完成
> 目标：导入导出 + Checklist + 对话存档
> 并行说明：三个任务无共享文件，全部可并行

### 8.1 REQ-2001 从导出文件导入创建新书

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2001-complex-import-from-export` |
| 优先级 | P1 |
| 工时 | 2h（14 tasks） |
| 核心改动 | 新 `server/src/modules/import/` 模块 + 导入对话框 + 解析器 |

**变更清单：**

- [ ] T1 shared：新增 `novelImport` 类型
- [ ] T2 后端：导入 API 端点
- [ ] T3 后端：导出文件解析器（JSON/ZIP）
- [ ] T4 后端：导入验证 + 冲突处理
- [ ] T5 前端：ImportFromFileDialog 组件
- [ ] T6 前端：导入进度 + 结果面板
- [ ] T7 测试：导入 API + 解析器

**验收标准：**

- [ ] 可拖拽/选择导出文件导入
- [ ] 导入后创建新书并包含所有章节
- [ ] 格式错误时有明确提示

### 8.2 REQ-3002 导演进度规划资源缺失 Checklist 可视化

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r3002-medium-director-planning-checklist` |
| 优先级 | P1 |
| 工时 | 1h（6 tasks） |
| 核心改动 | 导演面板新增资源缺失 Checklist |

**变更清单：**

- [ ] T1 shared：新增 checklist 投影类型
- [ ] T2 后端：`DirectorEventProjectionService` 生成 checklist
- [ ] T3 前端：`DirectorRuntimeProjectionCard` 渲染 checklist

**验收标准：**

- [ ] 自动检测缺失资源（角色/世界/流派等）
- [ ] Checklist 可交互（点击跳转到对应设置页）

### 8.3 REQ-3005 小说项目对话存档

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r3005-medium-novel-conversation-archive` |
| 优先级 | P1 |
| 工时 | 1.5h（10 tasks） |
| 核心改动 | 新对话存档页面 + API + WorkspaceRail 入口 |

**变更清单：**

- [ ] T1 后端：conversationArchiveService
- [ ] T2 后端：对话存档 CRUD API
- [ ] T3 前端：NovelConversationsPage 页面
- [ ] T4 前端：ConversationList + ConversationDetail 组件
- [ ] T5 前端：NovelWorkspaceRail 新增对话 Tab
- [ ] T6 测试：API + 前端渲染

**验收标准：**

- [ ] 可创建/编辑/删除对话记录
- [ ] 对话列表支持搜索和筛选
- [ ] WorkspaceRail 显示对话 Tab

### 8.4 复核实际完成度

- [ ] 逐项核对变更清单完成情况
- [ ] 核对验收标准全部达成
- [ ] 更新任务包 `tasks.md` 标记完成
- [ ] 更新任务包 `README.md` 状态

### 8.5 Block 8 测试闸门

```bash
pnpm typecheck
pnpm test
pnpm test:client
```

- [ ] 类型检查零错误
- [ ] 全量测试通过
- [ ] git commit

### 8.6 归档本阶段任务包

- [ ] 任务包从 `B.todo` 迁移至 `B.2.done`
- [ ] 同步更新 `docs_dev/1.task/requirements.md`
- [ ] 同步更新 `docs_dev/INDEX.md`

---

## 版本收口：全量验证 + 归档

> 前置：所有 Block 完成
> 目标：最终全量验证 + requirements.md 同步 + 全部提交

### 收口验证

```bash
pnpm typecheck                      # 全量类型检查
pnpm test                           # 后端单元测试
pnpm test:client                    # 前端测试
pnpm build                          # 全量构建
```

- [ ] 类型检查零错误
- [ ] 后端测试全部通过
- [ ] 前端测试全部通过
- [ ] 全量构建成功

### 收口文档

- [ ] `requirements.md` 全量同步（task 状态更新）
- [ ] `docs_dev/INDEX.md` 同步
- [ ] `docs_dev/1.task/README.md` 同步

### 收口提交

- [ ] git commit：`chore(v0.1): 版本收口 — 19 个任务包全部完成`

---

## 四、任务依赖矩阵

```text
任务       Block   依赖                可并行组
──────     ─────   ──────────────────  ──────
2005       1       无                  A（串行 2005→2008）
2008       1       2005                A
2006       2       无                  B（并行 2006∥3001）
3001       2       无                  B
2003       3       无                  C（并行 2003∥2007）
2007       3       无                  C
2009       4       2008                D（并行 2009∥2010∥2011）
2010       4       无                  D
2011       4       无                  D
2017       5       2008,2009           E（串行 2017→2002）
2002       5       2017                E
2004       6       无                  独立
2012       7       2017                F（2012∥2016 并行）
2016       7       无                  F
2015       7       无                  G（2015→3003 串行）
3003       7       2015                G
2001       8       无                  H（并行 2001∥3002∥3005）
3002       8       2012                H
3005       8       无                  H
```

---

## 五、进度追踪

| Block | 任务数 | 状态 | 通过测试？ | 已提交？ |
| --- | --- | --- | --- | --- |
| Block 1 | 2 | ✅ | ✅ | ✅ 7ba82a1 |
| Block 2 | 2 | ✅ | ✅ | ✅ ffc5919 |
| Block 3 | 2 | ✅ | ✅ | ✅ e77266e |
| Block 4 | 3 | ✅ | ✅ | ✅ 25ba868 |
| Block 5 | 2 | ✅ | ✅ | ✅ 9e17abc |
| Block 6 | 1 | ✅ | ✅ | ✅ (待提交) |
| Block 7 | 4 | ⬜ | ⬜ | ⬜ |
| Block 8 | 3 | ⬜ | ⬜ | ⬜ |
| 收口 | — | ⬜ | ⬜ | ⬜ |
