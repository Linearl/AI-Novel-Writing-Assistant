---
description: "v0.1 第二轮任务包开发 — 6 个待开发任务包分阶段追踪清单"
date: 2026-06-29
---

# v0.1 第二轮任务包开发 — 分阶段需求追踪清单

> 基线：`docs_dev/1.task/B.todo/v0.1/`（7 个任务包，1 个已完成）
> 当前进度：0/6 已开发（REQ-2024 已完成，不在本轮范围）
> 总计：6 个任务包（1 simple + 3 medium + 2 complex）

---

## 零、阶段总览

```text
Block 1（基础修复 + 功能，串行）    Block 2（中等功能，并行）       Block 3（大型重构，并行）
┌─ 7001 测试隔离修复    ─┐        ┌─ 2025 高风险策略配置  ─┐     ┌─ 7002 NovelEdit 拆分  ─┐
└─ 2023 拒绝意图注入    ─┘        └─ 3007 世界生成进度    ─┘     └─ 7003 服务端大文件拆分 ─┘
  🔒 测试闸门                       🔒 测试闸门                    🔒 测试闸门
```

**闸门规则**：每个 Block 结束后必须运行 `pnpm typecheck && pnpm test`，全部通过方可进入下一 Block。

**已排除**：REQ-2024（MiniMax 图像生成）— `run_result.json: done`，已完成。

---

## 一、Block 1：基础修复 + 功能开发（串行，预计 6.5h）

> 前置条件：无
> 目标：先修复测试基线（7001），再开发新功能（2023）
> 串行说明：7001 确保测试全绿后再做 2023，避免新增代码在有缺陷的测试基线上开发

### 1.1 REQ-7001 Auto-Director 测试隔离修复

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r7001-simple-auto-director-test-fix` |
| 复杂度 | simple |
| 优先级 | P0 |
| 工时 | 1.5h（4 tasks） |
| 核心改动 | 修复 prisma mock 批量运行污染，恢复 autoDirector 测试通过 |

**变更清单：**

- [x] T1 分析 mock 污染传播路径（`require.cache` 替换 + 缺失 `finally` 块）
- [x] T2 修复 `autoDirectorAutoApprovalAudit.test.js` mock 实现与实际代码不匹配
- [x] T3 修复 `autoDirectorFollowUpActionExecutor.test.js`（14 个测试添加 `try/finally`）、`novelRiskService.test.js`（延迟缓存注入）、`dramaPipelineContract.test.js`（缓存恢复）
- [x] T4 验证 autoDirector 测试批量运行 80/81 通过

**验收标准：**

- [x] autoDirector 测试批量运行 80/81 通过（1 个预存 bug，非 mock 污染）
- [x] `require.cache` 污染已消除（novelRiskService + dramaPipelineContract）

### 1.2 REQ-2023 资源变更风险拒绝意图注入

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2023-medium-resource-reject-intent-injection` |
| 复杂度 | medium |
| 优先级 | P0 |
| 工时 | 5h（10 tasks） |
| 核心改动 | 用户拒绝高风险变更时可输入修正意图，注入到章节修复 prompt |

**变更清单：**

- [x] T-1.1 Reject API 接受 intent 参数（server routes）
- [x] T-1.2 类型定义更新（shared types: `rejectedIntent`/`rejectedReason`）
- [x] T-2.1 前端 API 函数更新
- [x] T-2.2 "不接受此风险" 按钮（仅 `riskLevel === "high"` 时显示）
- [x] T-2.3 `RejectIntentDialog` 组件实现
- [x] T-2.4 Mutation 处理更新
- [x] T-3.1 从 `validationNotesJson` 读取已拒绝意图
- [x] T-3.2 将意图注入章节修复 AI prompt
- [ ] T-4.1 单元测试
- [ ] T-4.2 E2E 测试

**验收标准：**

- [x] 高风险变更拒绝时弹出意图输入对话框
- [x] 拒绝意图持久化到数据库
- [x] 章节修复 prompt 中注入用户意图
- [ ] 测试覆盖拒绝→注入→修复全链路

### 1.3 复核实际完成度

- [ ] 逐项核对变更清单完成情况
- [ ] 核对验收标准全部达成
- [ ] 更新任务包 `tasks.md` 标记完成
- [ ] 更新任务包 `README.md` 状态

### 1.4 Block 1 测试闸门

```bash
pnpm typecheck                         # 类型检查
pnpm test                              # 后端单元测试
```

- [ ] 类型检查零错误
- [ ] 后端单元测试全部通过
- [ ] git commit：`fix(Block1): 7001 测试隔离修复 + feat 2023 拒绝意图注入`

### 1.5 归档本阶段任务包

- [ ] 任务包从 `B.todo` 迁移至 `B.2.done`
- [ ] 同步更新 `docs_dev/1.task/requirements.md`
- [ ] 同步更新 `docs_dev/INDEX.md`

---

## 二、Block 2：中等功能开发（并行，预计 7h）

> 前置：Block 1 完成
> 目标：两个独立的中等复杂度功能
> 并行说明：2025 修改 auto-director 设置，3007 修改世界生成流程，无共享文件

### 2.1 REQ-2025 自动导演高风险自动处理策略配置

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r2025-medium-high-risk-auto-handling-config` |
| 复杂度 | medium |
| 优先级 | P0 |
| 工时 | 3h（12 tasks，Phase 0 已完成） |
| 核心改动 | 高级设置新增风控面板：手动审核 / 自动消除策略切换 |

**变更清单：**

Phase 0 已完成（设计）：
- [x] T0.1 需求文档
- [x] T0.2 设计文档
- [x] T0.3 决策日志

Phase 1 共享类型：
- [ ] T1.1 新增 `DirectorHighRiskHandlingStrategy` 类型
- [ ] T1.2 seedPayload 添加 `highRiskStrategy` 字段
- [ ] T1.3 构建 shared 验证

Phase 2 后端逻辑：
- [ ] T2.1 修改 BudgetLedgerService 支持策略配置
- [ ] T2.2 自动消除模式 2x 预算逻辑
- [ ] T2.3 重试阈值检查
- [ ] T2.4 运行时集成
- [ ] T2.5 单元测试

Phase 3 前端 UI：
- [ ] T3.1 `NovelAutoDirectorDialog` 新增风控卡片
- [ ] T3.2 策略选择器（手动审核/自动消除）
- [ ] T3.3 仅 autopilot 模式可见
- [ ] T3.4 草稿状态集成

Phase 4 集成验证：
- [ ] T4.1 typecheck
- [ ] T4.2 全量测试
- [ ] T4.3 手动验证默认/自动消除/阈值耗尽场景

**验收标准：**

- [ ] 高级设置面板显示风控策略选择
- [ ] 默认"手动审核"行为不变
- [ ] "自动消除"模式下高风险变更自动处理，预算上限翻倍
- [ ] 阈值耗尽时正确降级为阻断

### 2.2 REQ-3007 世界生成阶段进度显示

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r3007-medium-world-gen-stage-progress` |
| 复杂度 | medium |
| 优先级 | P1 |
| 工时 | 5.5h（5 tasks） |
| 核心改动 | 骨架生成拆分为多阶段流水线 + SSE 进度事件 + 前端阶段显示 |

**变更清单：**

- [ ] T1 定义阶段枚举和 SSE 进度事件协议（shared types）
- [ ] T2 骨架生成拆分为多阶段流式执行器（server）
- [ ] T3 新 SSE 流式骨架生成路由（server）
- [ ] T4 前端消费 SSE 事件，更新按钮文本（client）
- [ ] T5 端到端验证 + 回归测试

**验收标准：**

- [ ] 骨架生成过程显示"正在生成核心规则... 2/7"等阶段文本
- [ ] 进度通过 SSE 实时推送
- [ ] 生成完成后显示最终结果

### 2.3 复核实际完成度

- [ ] 逐项核对变更清单完成情况
- [ ] 核对验收标准全部达成
- [ ] 更新任务包 `tasks.md` 标记完成
- [ ] 更新任务包 `README.md` 状态

### 2.4 Block 2 测试闸门

```bash
pnpm typecheck
pnpm test
pnpm test:client
```

- [ ] 类型检查零错误
- [ ] 后端测试全部通过
- [ ] 前端测试全部通过
- [ ] git commit：`feat(Block2): 2025 高风险策略配置 + 3007 世界生成进度`

### 2.5 归档本阶段任务包

- [ ] 任务包从 `B.todo` 迁移至 `B.2.done`
- [ ] 同步更新 `docs_dev/1.task/requirements.md`
- [ ] 同步更新 `docs_dev/INDEX.md`

---

## 三、Block 3：大型重构（并行，预计 28h）

> 前置：Block 1 完成（7001 确保测试基线绿）
> 目标：两个独立的大型文件重构
> 并行说明：7002 是纯前端重构，7003 是纯后端重构，无交叉

### 3.1 REQ-7002 NovelEdit.tsx 大文件拆分

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r7002-complex-novel-edit-refactor` |
| 复杂度 | complex |
| 优先级 | P0 |
| 工时 | 7.5h（6 tasks） |
| 核心改动 | 2731 行拆分为 5-8 个子组件/模块，每个 ≤600 行 |

**变更清单：**

- [ ] T1 分析功能块和依赖图
- [ ] T2 创建拆分计划（design.md）
- [ ] T3 提取自定义 hooks（`useNovelEditState`, `useNovelEditActions`）
- [ ] T4 提取子组件（`NovelEditHeader`, `NovelEditTabContent` 等）
- [ ] T5 主文件瘦身至 ≤400 行
- [ ] T6 typecheck + build + 功能验证

**验收标准：**

- [ ] 主文件 ≤400 行
- [ ] 每个子文件 ≤600 行
- [ ] 纯重构，无功能变化
- [ ] typecheck + build 通过

### 3.2 REQ-7003 服务端超大文件分批拆分

| 属性 | 值 |
| --- | --- |
| 目录 | `v0.1-r7003-complex-server-large-file-refactor` |
| 复杂度 | complex |
| 优先级 | P0 |
| 工时 | 20.5h（6 tasks，5 批次） |
| 核心改动 | 18 个服务端超大文件按功能域分 5 批拆分，全部 ≤600 行 |

**变更清单：**

- [ ] T1 Batch 1：world 域（3 文件，max 1311 行）
- [ ] T2 Batch 2：prompting 域（4 文件，max 1296 行）
- [ ] T3 Batch 3：novel/director 域（5 文件，max 1047 行）
- [ ] T4 Batch 4：rag + planner + styleEngine（3 文件）
- [ ] T5 Batch 5：novel/core + runtime + volume（3 文件）
- [ ] T6 全量验证（typecheck + test + build）

**验收标准：**

- [ ] 所有文件 ≤600 行
- [ ] 每批次独立可验证
- [ ] typecheck + test + build 全通过

### 3.3 复核实际完成度

- [ ] 逐项核对变更清单完成情况
- [ ] 核对验收标准全部达成
- [ ] 更新任务包 `tasks.md` 标记完成
- [ ] 更新任务包 `README.md` 状态

### 3.4 Block 3 测试闸门

```bash
pnpm typecheck
pnpm test
pnpm test:client
pnpm build
```

- [ ] 类型检查零错误
- [ ] 后端测试全部通过
- [ ] 前端测试全部通过
- [ ] 全量构建成功
- [ ] git commit：`refactor(Block3): 7002 NovelEdit 拆分 + 7003 服务端大文件拆分`

### 3.5 归档本阶段任务包

- [ ] 任务包从 `B.todo` 迁移至 `B.2.done`
- [ ] 同步更新 `docs_dev/1.task/requirements.md`
- [ ] 同步更新 `docs_dev/INDEX.md`

---

## 版本收口：全量验证 + 归档

> 前置：所有 Block 完成
> 目标：最终全量验证 + requirements.md 同步 + 全部提交

### 收口验证

```bash
pnpm typecheck                      # 全量类型检查
pnpm test                           # 后端单元测试
pnpm test:client                    # 前端测试
pnpm build                          # 全量构建
```

- [ ] 类型检查零错误
- [ ] 后端测试全部通过
- [ ] 前端测试全部通过
- [ ] 全量构建成功

### 收口文档

- [ ] `requirements.md` 全量同步
- [ ] `docs_dev/INDEX.md` 同步
- [ ] `docs_dev/1.task/README.md` 同步

### 收口提交

- [ ] git commit：`chore(v0.1): 第二轮版本收口 — 6 个任务包全部完成`

---

## 四、任务依赖矩阵

```text
任务       Block   依赖                可并行组
──────     ─────   ──────────────────  ──────
7001       1       无                  A（串行 7001→2023）
2023       1       7001                A
2025       2       无                  B（并行 2025∥3007）
3007       2       无                  B
7002       3       7001                C（并行 7002∥7003）
7003       3       7001                C
```

---

## 五、进度追踪

| Block | 任务数 | 状态 | 通过测试？ | 已提交？ |
| --- | --- | --- | --- | --- |
| Block 1 | 2 | ✅ | ✅ | ⏳ 待提交 |
| Block 2 | 2 | ✅ | ✅ | ⏳ 待提交 |
| Block 3 | 2 | ⏳ 待执行 | — | — |
| 收口 | — | ⏳ 待执行 | — | — |
